{
  "name": "flight-lesson-rescheduler",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/app",
    "packages/weather-lib",
    "packages/functions/weather-monitor",
    "packages/functions/ai-rescheduler"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/app",
    "build": "npm run build --workspace=packages/app",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "prettier": "^3.0.0",
    "eslint": "^9.0.0"
  }
}
