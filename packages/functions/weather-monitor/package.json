{
  "name": "@flight-rescheduler/weather-monitor",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --external:@prisma/client --external:aws-sdk",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@flight-rescheduler/weather-lib": "*",
    "@prisma/client": "^5.0.0",
    "@aws-sdk/client-sqs": "^3.700.0",
    "date-fns": "^3.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "esbuild": "^0.24.0",
    "@types/node": "^20.0.0",
    "@types/aws-lambda": "^8.10.0",
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0"
  }
}
